import * as sinon from 'sinon';
import chai from 'chai';
const { expect } = chai;
import { stub } from 'sinon';

describe('Controllers', () => {

  before(async () => {
    res.status
  });

  after(()=>{
    sinon.restore();
  })

  it('', async () => {});

});